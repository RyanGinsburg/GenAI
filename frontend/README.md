# frontend

Placeholder. The React app gets scaffolded here at Step 6 of
[../BUILD_PROMPTS.md](../BUILD_PROMPTS.md).

**Node is not installed on this machine yet.** Before Step 6:

```bash
brew install node
```

Then the app gets scaffolded in place (Vite + React) and talks to the FastAPI
backend at `http://localhost:8000`.
